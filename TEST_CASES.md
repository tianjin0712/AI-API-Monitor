# AI API Monitor 测试用例

> 基于当前项目 V0.5-alpha 功能整理，共 50 个测试用例。重点覆盖日常使用主流程、常见误操作、网络与凭据异常、数据统计、桌面窗口行为和配置持久化。

## 1. 测试说明

- 测试平台：Windows 10/11；macOS 可复用除 Windows 托盘/凭据库表述外的大部分用例。
- 推荐准备：DeepSeek、OpenRouter、SiliconFlow 测试 Key 各 1 个；OpenAI/Claude 管理员 Key 按条件准备；已登录的 Codex CLI 环境 1 套。
- 涉及费用的平台优先使用低额度测试账户，不要在截图、日志或缺陷单中暴露完整 API Key。
- P0：核心流程或安全问题；P1：高频功能；P2：一般功能、兼容性或体验。
- 若真实平台暂时无法构造余额/额度边界，可通过测试数据库或 HTTP Mock 注入对应返回数据。

## 2. 启动与基础导航（TC-001～TC-005）

| 编号 | 优先级 | 场景 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-001 | P0 | 首次启动应用 | 清理测试环境中的应用数据；系统无该应用进程 | 1. 启动应用；2. 等待主界面加载完成 | 应用正常打开且无崩溃；默认进入“总览”；显示空账户引导；默认暗色主题；托盘出现 AI API Monitor 图标 |
| TC-002 | P1 | 在总览和设置之间切换 | 应用已启动 | 1. 点击“设置”；2. 再点击“总览”；3. 连续切换 3 次 | 页面内容与选中态正确切换；无白屏、重叠或明显卡顿；已填但未提交的设置表单在页面未卸载前表现稳定 |
| TC-003 | P1 | 无账户时查看总览 | 应用内没有 Provider | 打开“总览” | 页面提示“尚未添加 Provider”，并引导前往设置添加账户；不出现无意义的错误卡片或趋势图 |
| TC-004 | P1 | 应用重复启动后的数据可用性 | 已配置至少 1 个账户并保存布局 | 1. 正常退出应用；2. 重新启动；3. 查看总览与设置 | 应用能正常启动；账户、主题、布局、刷新策略及窗口设置仍存在；不会重复创建账户 |
| TC-005 | P2 | 长页面滚动与基本可读性 | 已配置多个账户，使总览或设置内容超过一屏 | 1. 滚动到页面底部；2. 再回到顶部；3. 切换总览/设置 | 滚动流畅；内容不被标题栏或导航遮挡；按钮、字段和提示文字可正常阅读与点击 |

## 3. Provider 账户管理（TC-006～TC-020）

| 编号 | 优先级 | 场景 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-006 | P0 | 添加日常使用的 DeepSeek 账户 | 准备有效 DeepSeek API Key | 1. 进入设置；2. 名称填“DeepSeek 日常”；3. 类型选 `deepseek`；4. 确认预置 URL；5. 输入 Key 并添加 | 添加成功；账户列表数量 +1；显示名称、`deepseek` 和正确 URL；返回总览后出现对应卡片 |
| TC-007 | P0 | 添加 OpenRouter 账户 | 准备有效 OpenRouter API Key | 选择 `openrouter`，填写名称和 Key，使用预置 URL 后添加 | 添加成功；刷新后能展示该 Key 对应的余额、今日/月度费用或平台实际可提供的数据 |
| TC-008 | P1 | 添加 SiliconFlow 账户 | 准备有效 SiliconFlow API Key | 选择 `siliconflow`，填写名称和 Key，使用预置 URL 后添加 | 添加成功；刷新后能展示余额；平台没有提供的字段显示“—”而不是伪造为 0 |
| TC-009 | P1 | 添加 OpenAI 管理员账户 | 准备具备组织 Usage/Costs 权限的 OpenAI 管理员 Key | 1. 选择 `openai`；2. 查看权限提示；3. 填写名称与 Key；4. 添加并刷新 | 表单明确提示需要组织管理员权限；添加成功；刷新后展示实际 Token/费用数据，余额未知时显示“—” |
| TC-010 | P1 | 普通 OpenAI 项目 Key 权限不足 | 准备无组织 Usage/Costs 权限的普通项目 Key | 添加账户后在总览刷新该账户 | 应用不崩溃；该账户卡片显示可读的 403/权限错误；其他账户数据不受影响 |
| TC-011 | P0 | 使用已登录的 Codex CLI 添加 Codex | 本机已执行 `codex login` 且登录有效 | 1. 类型选 `codex`；2. 观察表单；3. 填写名称并添加；4. 刷新 | Base URL 使用固定官方地址且不可编辑；无需 API Key 输入；添加成功；刷新后显示 Codex 额度/重置信息或可用数据 |
| TC-012 | P1 | Codex CLI 未登录或登录过期 | 本机无 `~/.codex/auth.json`，或凭据已失效 | 添加 Codex 账户并刷新 | 账户可保存；刷新失败时显示明确的登录/凭据错误，提示用户执行 `codex login`；不影响其他账户刷新 |
| TC-013 | P1 | 添加 Claude 管理员账户 | 准备 Anthropic Organization Admin Key | 1. 类型选 `claude`；2. 确认管理员 Key 提示；3. 添加并刷新 | 添加成功；可展示组织用量和费用；余额不可用时显示“—”，不把后付费账户错误显示为余额 0 |
| TC-014 | P1 | Provider 类型列表正确 | 无 | 打开类型下拉菜单，检查全部选项及顺序 | 仅出现当前已注册类型：`claude`、`codex`、`deepseek`、`openai`、`openrouter`、`siliconflow`；顺序稳定；不出现当前不可用的 `gemini` |
| TC-015 | P1 | 类型下拉键盘操作 | 焦点位于类型下拉按钮 | 1. 按 Enter 打开；2. 用上下方向键循环；3. 用 Home/End 跳转；4. 用空格选中；5. 再打开并按 Esc | 焦点与高亮项同步；首尾可循环；选择后菜单关闭且焦点回到触发按钮；Esc 关闭菜单并归还焦点 |
| TC-016 | P0 | 新增账户必填项校验 | 位于新增表单 | 依次尝试：名称为空、URL 为空、非 Codex 类型 Key 为空，然后点击“添加” | 不创建账户；分别出现清晰的必填错误；用户已输入的其他字段不被意外清空 |
| TC-017 | P0 | 阻止不安全的远程 HTTP 地址 | 准备任意非 Codex 类型与测试 Key | 将 Base URL 填为 `http://example.com` 并添加 | 添加失败；提示仅允许 HTTPS，HTTP 仅限本机回环地址；账户列表不新增记录，Key 不应留下孤立凭据 |
| TC-018 | P1 | 允许本机 HTTP 调试地址 | 本机启动兼容的 Mock 服务 | Base URL 分别使用 `http://localhost:端口` 或 `http://127.0.0.1:端口` 添加并刷新 | 地址校验通过并可创建账户；刷新请求发往本机服务；Mock 返回的数据被正确展示 |
| TC-019 | P0 | 编辑账户但不更换 API Key | 已有可正常刷新的非 Codex 账户 | 1. 点击“编辑”；2. 修改名称或 HTTPS URL；3. Key 留空；4. 保存；5. 刷新 | 修改成功；原 API Key 继续有效；名称/URL 更新；Key 留空不会清除或替换原凭据 |
| TC-020 | P0 | 编辑账户并更换 API Key | 已有账户，准备另一个有效 Key | 1. 编辑账户；2. 输入新 Key；3. 保存并刷新 | 新 Key 生效；旧 Key 不再被该账户使用；设置列表和前端接口均不展示 Key 明文 |

## 4. 删除、刷新与异常恢复（TC-021～TC-032）

| 编号 | 优先级 | 场景 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-021 | P1 | 取消删除账户 | 已有至少 1 个账户 | 1. 点击“删除”；2. 在确认框选择取消 | 账户仍在列表中；总览卡片和历史数据关联不受影响；API Key 未被清除 |
| TC-022 | P0 | 确认删除账户及凭据 | 已有测试账户 | 1. 点击“删除”；2. 确认删除；3. 返回总览；4. 重启应用 | 账户从设置和总览消失，重启后不恢复；对应系统凭据被清理；其他账户不受影响 |
| TC-023 | P1 | 删除凭据清理失败可见 | 通过 Mock/权限限制制造系统凭据删除失败 | 确认删除账户 | 数据库账户仍被删除；界面明确提示凭据可能残留及清理失败原因，不静默宣称完全成功 |
| TC-024 | P0 | 启动后自动刷新所有账户 | 已有 2 个以上有效账户 | 启动应用并停留在总览 | 各账户开始刷新；成功数据分别匹配正确账户，不串卡；刷新完成后显示余额、Token、费用和更新时间中的可用字段 |
| TC-025 | P1 | 手动立即刷新全部账户 | 已有多个账户 | 点击账户列表上方“立即刷新” | 按钮显示“刷新中…”并暂时不可重复点击；所有账户发起刷新；完成后按钮恢复，数据与更新时间更新 |
| TC-026 | P1 | 只刷新单个账户 | 已有多个账户且均完成过刷新 | 记录各卡片更新时间，只点击其中一张卡片的刷新按钮 | 仅目标卡片进入刷新态并更新；其他卡片数据和更新时间保持不变；刷新按钮不会触发卡片外的意外操作 |
| TC-027 | P0 | 多账户部分刷新失败 | 配置 1 个有效 Key、1 个无效 Key、1 个不可达 URL | 点击“立即刷新” | 有效账户正常更新；失败账户各自显示错误；一次失败不阻断其他账户；不会用失败结果覆盖其他账户数据 |
| TC-028 | P1 | 断网时刷新 | 已有至少 1 个成功刷新过的账户 | 1. 断开网络；2. 手动刷新；3. 恢复网络后再次刷新 | 断网时显示 HTTP/网络错误且应用不假死；已有旧数据不被错误串改；联网后可正常恢复刷新 |
| TC-029 | P1 | API Key 被撤销或过期 | 已有账户，之后在平台侧撤销其 Key | 回到应用刷新对应账户 | 卡片显示认证失败信息；应用不泄露完整 Key；编辑并换入新 Key 后刷新恢复正常 |
| TC-030 | P1 | 快速连续点击避免重复请求 | 已有多个账户 | 快速连续点击“立即刷新”及单卡刷新按钮 | 刷新期间重复操作被禁用或忽略；同一时刻不会叠加多轮刷新；界面最终回到可操作状态 |
| TC-031 | P1 | 前后台刷新间隔的日常设置 | 已有账户 | 1. 设置前台 30 秒、后台 120 秒；2. 保存；3. 前台观察两轮；4. 将窗口切到后台再观察 | 保存成功；前台约按 30 秒刷新，后台约按 120 秒刷新；应用切回前台/获得焦点后立即刷新一次 |
| TC-032 | P0 | 刷新间隔边界与非法组合 | 位于设置页 | 分别尝试：前台 9、前台 3601、后台 59、后台 3601、后台小于前台；再测试 10/60 与 3600/3600 | 非法值被拒绝并显示对应范围或逻辑错误；合法边界可保存；失败保存不会覆盖上一次有效配置 |

## 5. 数据展示、趋势、预测与提醒（TC-033～TC-041）

| 编号 | 优先级 | 场景 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-033 | P1 | 账户卡片数据格式 | Mock 或真实账户返回余额、Token、今日费用、剩余百分比和更新时间 | 刷新并查看卡片 | 金额、币种、Token 缩写/千分位、相对时间格式易读；剩余百分比限制在 0～100% 的视觉范围；字段不会互相错位 |
| TC-034 | P1 | 平台不提供某字段 | 使用仅返回部分数据的平台账户 | 刷新并查看账户卡片、今日汇总和费用概览 | 未知余额/费用显示“—”或“暂无数据”，不伪装成 0；已知字段照常展示；汇总不把未知值误算为实际零消费 |
| TC-035 | P1 | 今日汇总与费用概览按币种聚合 | 准备多个同币种账户，并可选加入另一币种账户 | 刷新所有账户，手工计算余额、今日费用、30 天费用与 Token 合计 | 账户数正确；相同币种正确求和；不同币种分开展示，不直接混加；Token 合计与各卡片一致 |
| TC-036 | P1 | 趋势历史不足 | 某账户只有 0～1 天历史记录 | 在“消耗趋势”查看该账户 | 显示“至少需要 2 天记录”的说明，不绘制误导性折线；应用无报错 |
| TC-037 | P1 | 切换趋势账户和指标 | 至少 2 个账户，各有不少于 2 天历史 | 1. 在趋势中切换账户；2. 在“费用”和“Token”间切换 | 图表切换到对应账户和指标；不会短暂显示已删除/上一个账户的旧数据；数据点与历史记录相符 |
| TC-038 | P1 | 正常生成消耗预测 | 某余额型账户近 7 天有多个真实费用样本且余额大于 0 | 查看趋势区预测数据 | 显示日均消耗、预计剩余天数、预计耗尽日期、样本数和覆盖天数；日均按“有费用数据的日期”计算，不把缺失日当 0 |
| TC-039 | P1 | 无法预测时的降级说明 | 账户无余额、无有效费用样本，或日均费用为 0 | 查看趋势区 | 历史仍可正常展示；提示当前无法预测耗尽时间；预计剩余/耗尽不显示虚假日期或无穷大 |
| TC-040 | P0 | 低额度系统提醒与去重 | 已授予系统通知权限；通过 Mock 依次返回剩余 29%、29%、9%、50%、29% | 每次返回后刷新账户 | 首次 29% 发黄色低额度通知；再次 29% 不重复通知；降至 9% 发严重不足通知；恢复至 50% 后状态重置；再次跌至 29% 可再次提醒 |
| TC-041 | P1 | 无额度百分比时按预测天数提醒 | 平台不返回 remaining；Mock/历史数据可形成 6 天、2 天、8 天预测 | 依次刷新并观察通知 | 预计不足 7 天时发黄色提醒，不足 3 天时发红色提醒；恢复到 8 天后重置；通知中包含正确账户名和预计天数 |

## 6. 布局与主题（TC-042～TC-046）

| 编号 | 优先级 | 场景 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-042 | P1 | 暗色/亮色主题切换并持久化 | 应用处于 Full 模式 | 1. 点击标题栏主题按钮；2. 检查总览和设置；3. 重启应用 | 两个页面同步切换主题；文字、输入框、卡片和图表保持可读；重启后恢复最后选择的主题 |
| TC-043 | P1 | 隐藏与重新显示 Widget | 位于总览 | 1. 点击“编辑布局”；2. 隐藏“费用概览”；3. 完成编辑；4. 再次编辑并显示 | 非编辑模式下该 Widget 隐藏；再次显示后内容恢复；其他 Widget 的数据与顺序不受影响 |
| TC-044 | P1 | 拖动调整 Widget 顺序 | 位于布局编辑模式 | 使用拖动把手将“消耗趋势”移到顶部，完成编辑并重启 | 仅拖动把手可排序；完成后顺序正确；重启后仍保持新顺序；卡片内按钮不会误触发拖拽 |
| TC-045 | P1 | 恢复默认布局 | 已调整顺序并隐藏部分 Widget | 进入编辑布局，点击“恢复默认”，再完成编辑 | 四类 Widget 恢复默认顺序且全部显示；主题不被意外重置 |
| TC-046 | P1 | 布局保存失败与重试 | 通过只读数据库/Mock 制造一次保存失败，之后恢复 | 1. 修改布局；2. 等待保存；3. 查看失败提示；4. 恢复条件并点击“重试” | 保存失败提示清晰可见；不静默丢失；重试成功后提示消失；重启后加载成功保存的布局 |

## 7. 窗口、托盘与安全持久化（TC-047～TC-050）

| 编号 | 优先级 | 场景 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-047 | P1 | Full、Mini、小球模式切换 | 应用处于 Full 模式 | 1. 点击 Mini 按钮；2. 点击 Mini 条展开；3. 切到小球；4. 点击小球展开 | Full 可缩放；Mini 约为 280×96 且可拖动；小球约为 72×72 且可拖动；点击 Mini/小球均回到 Full；模式切换无多余窗口 |
| TC-048 | P1 | 窗口位置、大小与多显示器恢复 | 有双显示器条件更佳 | 1. 分别移动 Full/Mini/小球到不同位置；2. 调整 Full 大小；3. 重启；4. 再断开副屏并启动 | 各模式恢复各自保存的位置；Full 恢复大小；副屏存在时允许负坐标；副屏断开后窗口不会永久出现在屏幕外 |
| TC-049 | P0 | 关闭到托盘、显示隐藏与真正退出 | 应用正在运行且托盘可见 | 1. 点击窗口关闭；2. 左键单击托盘两次；3. 右键依次测试显示、隐藏和模式切换；4. 点击“退出” | 关闭按钮仅隐藏到托盘，进程仍在；托盘左键切换显隐；右键菜单动作正确并聚焦窗口；只有“退出”真正结束进程 |
| TC-050 | P0 | 置顶与敏感数据安全持久化 | 已添加两个同名但 Key 不同的账户 | 1. 开启 Always On Top 并切换其他窗口验证；2. 重启后复查；3. 查看设置列表、SQLite 和常规日志；4. 分别刷新两个同名账户 | 置顶立即生效且重启后保留；关闭后可正常取消；API Key 不出现在 UI、SQLite 或普通日志中；两个同名账户使用各自独立凭据，不互相覆盖 |

## 8. 建议执行顺序

1. 每次发布前冒烟：TC-001、TC-006、TC-011、TC-024、TC-025、TC-027、TC-042、TC-047、TC-049、TC-050。
2. Provider 回归：TC-006～TC-020、TC-024～TC-030。
3. 桌面体验回归：TC-031、TC-042～TC-050。
4. 高级统计回归：TC-033～TC-041，优先使用固定 Mock 数据保证结果可重复。

