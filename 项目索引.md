# AI API Monitor 项目索引

> 扫描基线：2026-08-13；项目版本：`0.1.0` / 文档标识 `V0.5-alpha`。  
> 扫描范围：项目根目录下 63 个源码、配置、资源与文档文件。已忽略 `.git/`、`node_modules/`、`dist/`、`.reasonix/` 等版本库元数据、依赖、构建产物和会话元数据。本文只记录静态扫描结果，未运行构建、测试或真实平台 API。

## 一、项目概览

| 项目 | 扫描结果 |
|---|---|
| 项目名称 | AI API Monitor（包名 `ai-api-monitor`） |
| 项目类型 | 跨平台桌面 AI API 用量监控工具 |
| 应用标识 | `com.aiapimonitor.desktop` |
| 桌面框架 | Tauri 2，前端运行于系统 WebView |
| 前端 | React 19、TypeScript 5.8、Vite 7、Tailwind CSS 4 |
| 后端 | Rust 2021 edition、Tokio、Reqwest、Serde |
| 数据与凭据 | SQLite（rusqlite，WAL，schema v4）+ 系统凭据库（keyring） |
| 包管理 | pnpm workspace；锁文件为 `pnpm-lock.yaml` |
| 当前状态 | V0.5-alpha；核心监控、桌面窗口与统计能力已实现，已有前端单元测试与 CI 基线，仍缺少真实账户 E2E 和 Provider HTTP mock 合约测试 |

开发机必须具备受支持的 Node.js（20.19+、22.12+ 或 24+）、pnpm `11.21.0`、Rust/Cargo `1.97.1` 及 Tauri 2 所需系统依赖。Windows 需要 WebView2；`tauri.conf.json` 配置 `targets: all`，但仓库测试说明和 CI 主要面向 Windows。

已注册并可在 UI 添加的 Provider：`claude`、`codex`、`deepseek`、`openai`、`openrouter`、`siliconflow`。`gemini.rs` 存在，但因没有公开余额/用量端点而未注册。

## 二、目录结构索引

```text
Token_Number/
├── src/                            # React/TypeScript 前端
│   ├── components/
│   │   ├── MiniBall.tsx            # Mini 条与悬浮球界面
│   │   ├── ProviderCard.tsx        # 单账户用量卡片
│   │   ├── TitleBar.tsx            # 无边框标题栏与窗口按钮
│   │   └── TrendWidget.tsx         # 历史折线与余额耗尽预测
│   ├── pages/
│   │   ├── Dashboard.tsx           # 总览、刷新调度、Widget 编排
│   │   └── Settings.tsx            # Provider CRUD、刷新与窗口设置
│   ├── utils/
│   │   ├── format.ts               # 数字、金额、相对时间格式化
│   │   └── layout.ts               # 布局默认值、解析与向前兼容
│   ├── api.ts                      # Tauri invoke 的唯一前端封装
│   ├── App.tsx                     # 页面/窗口模式/主题/布局根状态
│   ├── index.css                   # Tailwind 入口、主题和组件样式
│   ├── main.tsx                    # React 挂载与主题预加载
│   ├── types.ts                    # 前后端传输对象的 TS 镜像
│   └── vite-env.d.ts               # Vite 类型声明
├── src-tauri/                      # Tauri/Rust 后端
│   ├── capabilities/default.json   # 主窗口权限能力清单
│   ├── icons/                      # 桌面及各平台安装包图标
│   ├── src/
│   │   ├── db/mod.rs               # SQLite 连接与 v1-v4 迁移
│   │   ├── providers/
│   │   │   ├── mod.rs              # Provider trait、统一模型、注册表
│   │   │   ├── claude.rs           # Anthropic Admin Usage/Cost
│   │   │   ├── codex.rs            # Codex CLI 登录态与内部额度接口
│   │   │   ├── deepseek.rs         # DeepSeek 余额
│   │   │   ├── gemini.rs           # 未注册占位实现
│   │   │   ├── openai.rs           # OpenAI Usage/Costs 分页聚合
│   │   │   ├── openrouter.rs       # OpenRouter Key 限额/费用
│   │   │   └── siliconflow.rs       # SiliconFlow 余额（实验性端点）
│   │   ├── commands.rs             # Tauri 命令、刷新、历史、预测、提醒
│   │   ├── lib.rs                  # 应用装配、托盘、关闭行为、事件
│   │   ├── main.rs                 # 原生可执行入口
│   │   ├── settings.rs             # Provider/设置 CRUD 与输入校验
│   │   ├── storage.rs              # keyring 安全凭据读写
│   │   └── window_mode.rs          # Full/Mini/Ball 状态与几何恢复
│   ├── build.rs                    # Tauri 构建脚本
│   ├── Cargo.toml / Cargo.lock     # Rust 依赖与锁文件
│   └── tauri.conf.json             # 窗口、安全策略和打包配置
├── public/                         # Web 静态 SVG
├── index.html                      # WebView HTML 入口
├── package.json / pnpm-lock.yaml   # 前端依赖、脚本与锁文件
├── vite.config.ts                  # Vite/Tauri 开发服务器配置
├── tsconfig*.json                  # TypeScript 严格模式配置
├── README.md                       # 现有用户/开发说明
├── mission.md                      # 产品目标与规划
├── TEST_CASES.md                   # 50 条手工/集成测试场景
└── codereview.md                   # 历史审查与修复记录
```

补充说明：`.vscode/extensions.json` 是编辑器推荐配置；`.gitignore` 已排除依赖、日志、构建目录和 `.reasonix/`。`dist/` 是前端构建产物、`node_modules/` 是依赖缓存，均不属于源码索引。

## 三、核心代码索引

| 文件 | 类型/核心对象 | 功能 | 主要关联模块 |
|---|---|---|---|
| `src/main.tsx` | React 入口 | 读取主题缓存并挂载应用 | `App.tsx`、`index.css` |
| `src/App.tsx` | `App`、`Layout` | 页面切换、Full/Mini/Ball 渲染、主题与布局持久化 | `api.ts`、Dashboard、Settings、TitleBar、MiniBall |
| `src/api.ts` | `api` | 17 个 Tauri 命令的类型化调用边界 | `types.ts`、Rust `commands.rs` |
| `src/pages/Dashboard.tsx` | `Dashboard` | Provider 加载、前后台定时刷新、单飞控制、Widget 排序 | ProviderCard、TrendWidget、`commands.rs` |
| `src/pages/Settings.tsx` | `Settings` | Provider 增删改、类型选择、刷新间隔、置顶 | `api.ts`、`settings.rs`、`window_mode.rs` |
| `src/components/TrendWidget.tsx` | `TrendWidget` | 查询历史/预测并绘制 SVG 折线 | `get_usage_history`、`get_prediction` |
| `src/utils/layout.ts` | `parseWidgets`、`parseTheme` | 布局容错、默认 Widget 合并 | `App.tsx`、Dashboard |
| `src-tauri/src/lib.rs` | `run` | 注册插件、状态、命令、托盘和窗口事件 | 全部 Rust 模块 |
| `src-tauri/src/commands.rs` | Tauri commands | 命令编排、并发刷新、历史 UPSERT、预测和通知 | ProviderManager、Db、settings、window_mode |
| `src-tauri/src/providers/mod.rs` | `ProviderAdapter`、`ProviderManager` | 统一用量模型与适配器注册表 | 六个已注册适配器 |
| `src-tauri/src/settings.rs` | `AppError`、CRUD 函数 | 输入/URL 校验、DB 操作、凭据补偿和旧凭据迁移 | Db、SecureStorage、ProviderManager |
| `src-tauri/src/storage.rs` | `SecureStorage` | 以 UUID 账户名读写系统凭据库 | settings、commands |
| `src-tauri/src/db/mod.rs` | `Db`、`migrate` | SQLite WAL、外键、schema v1-v4 | settings、commands、window_mode |
| `src-tauri/src/window_mode.rs` | `WindowMode`、`WindowState` | 三模式尺寸/位置、置顶与跨显示器恢复 | lib、commands、settings |

主要调用链：

```text
React 页面/组件
  -> src/api.ts invoke
  -> commands.rs 命令编排
  -> settings.rs / window_mode.rs / ProviderManager
  -> ProviderAdapter -> 外部平台 API
  -> usage_history(SQLite) / keyring(系统凭据库)
  -> ProviderUsage / RefreshResult 返回前端
```

数据库关系：`providers` 保存账户元数据与 `key_ref`；`usage_history` 通过 `provider_id` 外键关联并按 `(provider_id, date)` 唯一 UPSERT；`settings` 保存刷新策略、布局和窗口状态。删除 Provider 时历史记录级联删除。

## 四、资源文件索引

| 资源 | 路径 | 用途/使用状态 | 重复判断 |
|---|---|---|---|
| 产品 favicon | `public/favicon.svg` | `index.html` 产品图标；正在使用 | 唯一产品 SVG |
| 打包图标 | `src-tauri/icons/32x32.png`、`128x128.png`、`128x128@2x.png`、`icon.icns`、`icon.ico` | 被 `tauri.conf.json` 明确引用；正在使用 | 各文件哈希不同，属于尺寸/平台变体 |
| 其他图标 | `icon.png`、`Square*Logo.png`、`StoreLogo.png` | 未被当前 `tauri.conf.json` 明确引用；可能是 Tauri 生成的 Windows 商店资源，用途无法仅凭静态配置完全确认，标记“未知” | 各文件哈希不同，无完全重复 |
| UI 图形 | TSX 内联 SVG | 标题栏、悬浮球和趋势图；正在使用 | 代码内资源，无独立文件 |
| 音频/模型/字体/数据文件 | 无 | 扫描未发现 | 不适用 |
| 运行时配置 | `tauri.conf.json`、`capabilities/default.json`、`vite.config.ts`、`tsconfig*.json` | 构建、权限、安全策略和类型检查；正在使用 | 无重复 |

## 五、项目功能模块分析

```text
Provider 管理
├── 六类账户注册与动态类型列表
├── 新增、编辑、删除与 URL 校验
├── keyring 安全凭据
└── Codex CLI 本地登录态复用

监控与统计
├── 单账户/全账户刷新
├── 前后台定时调度与窗口聚焦刷新
├── SQLite 每日历史
├── Token/费用趋势
├── 余额耗尽预测
└── 额度分级系统通知

桌面体验
├── Full / Mini / Ball
├── 无边框拖拽与窗口几何恢复
├── Always On Top
├── 托盘显隐/切换/退出
└── 深浅主题与 Widget 布局
```

| 模块 | 实现情况 | 相关文件 | 已知问题/限制 |
|---|---|---|---|
| Provider CRUD | 已实现，后端白名单和 HTTPS/回环 URL 校验 | Settings、settings.rs | Provider 预置 URL 同时存在于前端和各适配器，新增平台时需同步维护 |
| Provider 采集 | 六个平台已注册；Gemini 明确禁用 | providers/*.rs | Codex 使用未公开内部接口；SiliconFlow 端点无正式文档；OpenAI/Claude 需要管理员权限 |
| 安全存储 | API Key 不序列化到前端或 SQLite，失败有补偿逻辑 | storage.rs、settings.rs | 依赖系统凭据服务可用性；启动迁移失败需人工重录 |
| 刷新调度 | 前后台间隔、聚焦刷新、单飞、后端最多 3 并发 | Dashboard、commands.rs | 趋势组件不会在同页刷新成功后主动重载历史 |
| 历史与预测 | schema v4，未知费用保存为 NULL，近 7/30 天统计 | db、commands、TrendWidget | “无法预测”提示条件与后端返回模型不完全匹配 |
| 额度通知 | Warning/Critical 分级和恢复后重置 | commands.rs | 进程重启后去重状态清空，低额度可能每次启动首次刷新都通知 |
| 桌面窗口 | 三模式、托盘、置顶、几何持久化已实现 | lib.rs、window_mode.rs | 主要依赖手工桌面测试；无自动化窗口测试 |
| DIY UI | 主题、Widget 显隐、HTML5 拖拽排序 | App、Dashboard、layout.ts | 仍不含自由定位、缩放、透明度等规划能力；HTML5 拖拽对触控不友好 |

## 六、代码质量分析

当前有 46 个 Rust 测试和 6 个 Vitest 前端测试，覆盖迁移、校验、适配器解析、统计、窗口纯函数、预测提示与布局兼容。`pnpm check` 统一执行类型检查、前端测试、Rust 格式、严格 Clippy 和 Rust 测试，Windows CI 会自动执行该门禁。

| 严重度 | 位置 | 问题 | 原因/影响 |
|---|---|---|---|
| 高 | `src-tauri/src/lib.rs` 数据库初始化 | `Db::open(...).expect(...)` 在数据库损坏、目录权限或锁异常时直接终止进程 | 应用无法启动且没有用户可理解的恢复路径 |
| 中 | `src/components/TrendWidget.tsx` | 刷新用量后趋势不自动重新查询；只有账户选择/组件重挂载会触发 `load` | 同一总览页面可能继续显示旧趋势和旧预测 |
| 中 | `TrendWidget.tsx:142-168` 与 `commands.rs::predict_for` | Provider 存在时后端通常返回 `Some(Prediction)`，即使样本为 0；前端的降级文案却只在 `!prediction` 时出现 | 无样本/无余额时仅显示 `0.00` 和破折号，用户看不到“无法预测”的原因 |
| 中 | `src-tauri/src/providers/openrouter.rs` | `key_url` 无法识别用户已填 `/api/v1` 的 Base URL，会再追加 `/api/v1/key` | 自定义常见 Base URL 可能生成重复路径并刷新失败 |
| 中 | 测试配置 | 已有前端测试与 CI 基线，但仍缺少组件交互、计时器和 Provider HTTP mock 集成测试 | UI 竞态、可访问性和平台返回变化仍可能遗漏 |
| 低 | `src/types.ts` 与 Rust DTO | 前后端类型采用手工双份定义 | 字段新增或可空性变化时可能漂移；当前扫描未发现明显不一致 |
| 低 | `commands.rs` / 多个组件文件 | `commands.rs` 783 行，Dashboard/Settings 均超过 430 行 | 职责较多，后续迭代的审查与测试成本上升 |
| 低 | `record_usage`、预测日期 | 历史日期和耗尽日期使用 UTC 日历日 | 非 UTC 时区的午夜附近可能与用户本地“今日”认知不一致；需先明确产品口径 |
| 低 | `package.json`、工具链 | 未设置 `packageManager`、Node engines、`rust-toolchain.toml` | 环境复现依赖人工约定 |

未发现：源码中的 `unsafe`、API Key 明文落库、完全相同的二进制资源、明显复制粘贴的 Provider 实现。各 Provider 的 HTTP 公共流程相似，但因认证、分页和响应模型差异较大，当前独立适配器结构合理。

## 七、项目优化建议

完整的可执行建议见 [`优化建议.md`](./优化建议.md)。摘要如下：

- P0：CI 门禁和数据库诊断错误已完成；下一步为数据库故障增加用户可操作的恢复界面。
- P1：趋势联动、预测提示、OpenRouter URL 和前端测试基线已完成；继续补组件调度与 HTTP mock 测试。
- P2：拆分大型文件、生成前后端合约类型、固定工具链、清理脚手架资源并明确时区口径。

## 八、开发路线规划

### 短期（1～7 天）

1. 修复趋势刷新与预测降级提示，并增加对应前端测试。
2. 为数据库启动失败提供可读错误和恢复/备份指引。
3. 增加 `typecheck`、`cargo fmt --check`、`cargo clippy`、Rust test 的统一检查入口。
4. 修正应用 HTML 标题/favicon，确认并清理未用 SVG。
5. 用固定 HTTP mock 回归六个 Provider 的成功、权限不足、超时和异常响应。

### 中期（1 个月）

1. 引入前端组件测试和 Tauri 命令集成测试，自动化覆盖 `TEST_CASES.md` 的 P0/P1 主路径。
2. 拆分 Dashboard/Settings/commands，形成刷新服务、统计服务、通知服务和表单子组件。
3. 建立 Provider 合约样本库，监测外部 API 字段变化。
4. 完善日志与诊断导出，但确保凭据和响应敏感字段脱敏。
5. 完成 Windows 真实环境回归，并选择性验证 macOS/Linux 打包。

### 长期

1. 用稳定公开接口替换 Codex 内部接口；对实验性 Provider 建立能力标记。
2. 将 Provider 能力（余额、Token、费用、重置）显式建模，减少 UI 对 `null` 的隐式推断。
3. 实现完整 DIY 布局、可访问的键盘排序和触控排序。
4. 形成版本化数据导出/备份、数据库恢复与迁移回滚方案。

## 九、维护文档

项目介绍、环境、启动、规范和后续计划已整理至 [`README_Project.md`](./README_Project.md)。优化任务的优先级、位置、方案与收益已独立整理至 [`优化建议.md`](./优化建议.md)。
